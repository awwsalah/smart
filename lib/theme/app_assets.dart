/// Bundled image paths (declare in pubspec.yaml).
class AppAssets {
  AppAssets._();

  /// Main in-app background (homes, forms, profile, etc.).
  static const backgroundApp = 'assets/images/bg.jpg';

  /// Auth / splash background (splash + login).
  static const backgroundAuth = 'assets/images/bg2.jpg';
}
